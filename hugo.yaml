# -------------------------------------------------------------------
# Basic hugo configuration settings
# - See also: https://gohugo.io/getting-started/configuration/#all-configuration-settings
# -------------------------------------------------------------------
# For default settings, see: 
# - config/_default/markup.yaml
# - config/_default/menu.yaml
# - config/_default/params.yaml
# - config/_default/server.yaml
# - config/_default/taxonomies.yaml
# -------------------------------------------------------------------
# Base Config
title: Your Website
baseURL: ''

# Import module dependencies used by this theme
module:
  hugoVersion: { min: "0.112.7", extended: true }
  imports:
    - path: "github.com/gohugoio/hugo-mod-jslibs-dist/alpinejs/v3"

